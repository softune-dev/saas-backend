uvicorn app.main:app --reload
python -m app.worker

docker compose up -d

cd dashboard
cd landing

cd templates/aurora
cd templates/bazaar
npx next dev -p 3050
npx next dev -p 3052

aurora-owner@softune.app
12345679



cd ~/saas-backend && git pull && docker compose -f docker-compose.prod.yml up -d --build api worker
docker compose -f docker-compose.prod.yml ps

netstat -ano | findstr :PORT
taskkill /F /PID {PORT}

